apiVersion: v1
kind: Service
metadata:
  name: colabri-service
  namespace: colabri
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http

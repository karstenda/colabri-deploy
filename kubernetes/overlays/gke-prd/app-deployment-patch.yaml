apiVersion: apps/v1
kind: Deployment
metadata:
  name: colabri-app
  namespace: colabri
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: colabri-app
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: colabri-secrets
                  key: db-url
            - name: SECURE_COOKIES
              value: "true"
            - name: CLOUD_AUTH_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: colabri-secrets
                  key: cloud-auth-jwt-secret
            - name: CLOUD_SERVICE_DOMAIN
              value: "app.colabri.cloud"
            - name: CLOUD_DOC_SERVICE_DOMAIN
              value: "colabri-doc-service"
            - name: CLOUD_SPELL_SERVICE_DOMAIN
              value: "colabri-spell-service"
            - name: CLOUD_ROOT_DOMAIN
              value: "colabri.cloud"
            - name: CLOUD_CORS_ORIGINS
              value: "https://*.colabri.cloud,https://colabri.cloud"
            - name: OAUTH_WEB_APP_CALLBACK_URL
              value: "https://app.colabri.cloud/auth/callback"
            - name: GCP_PROJECT_ID
              value: colabri-prd
            - name: GCP_SERVICE_ACCOUNT_KEY_PATH
              value: /var/secrets/google/gcp-sa-key.json
          volumeMounts:
            - name: gcp-credentials
              mountPath: /var/secrets/google
              readOnly: true
            - name: appconfig
              mountPath: /app/static/appconfig.js
              subPath: appconfig.js
              readOnly: true
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
        - name: gcp-credentials
          secret:
            secretName: colabri-secrets
            items:
              - key: gcp-sa-key.json
                path: gcp-sa-key.json
        - name: appconfig
          secret:
            secretName: colabri-secrets
            items:
              - key: appconfig.js
                path: appconfig.js
